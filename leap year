#include <stdio.h>

int main() {
    int a;
    scanf("%d",&a);
    if(a%4==0 &&(1%100!=0|| a%400==0))
    {
        printf("Leap Year");
        
    }
    else{
        printf("not a Leap Year");
    }
}
