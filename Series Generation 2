Write a program to generate the following series 7, 5, 8, 6, 9….

Input Format

Read n value

Constraints

1<=n<=10^7

Output Format

Print series

Sample Input 0

5
Sample Output 0

7 5 8 6 9
Sample Input 1

10
Sample Output 1

7 5 8 6 9 7 10 8 11 9

#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    // Initial values of the series
    int a = 7, b = 5;
    
    // Print the first two elements
    printf("%d %d ", a, b);

    // Generate and print the remaining elements of the series
    for (int i = 3; i <= n; i++) {
        if (i % 2 == 1) {
            a = a + 1;
            printf("%d ", a);
        } else {
            b = b + 1;
            printf("%d ", b);
        }
    }

    return 0;


